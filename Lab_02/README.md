# Airflow Lab

This lab actually demonstrates how Apache Airflow is used to automate a machine learning workflow.

The pipeline loads e-commerce customer data, preprocesses it, trains a K-Means clustering model, finds the optimal number of clusters using elbow method, and generates visualizations.
